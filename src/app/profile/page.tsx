import { candidateProfile as profile } from "@/data/candidate-profile";

const money = new Intl.NumberFormat("en-GB", { style: "currency", currency: "GBP", maximumFractionDigits: 0 });

export default function ProfilePage() {
  return <main className="main"><div className="top"><div><span className="eyebrow">Candidate intelligence</span><h1>Your career profile</h1><p>This baseline controls how every role is ranked and explained.</p></div><button className="button">Upload updated CV</button></div><section className="profile-grid"><div className="jobs"><section className="card"><span className="eyebrow">Current position</span><h2>{profile.currentRole}</h2><p>{profile.currentEmployer} · {profile.location}</p><div className="profile-facts"><div><small>Current salary</small><strong>{money.format(profile.currentSalary)}</strong></div><div><small>Application floor</small><strong>{money.format(profile.minimumSalary)}</strong></div><div><small>Maximum commute</small><strong>{profile.preferences.maxCommuteMinutes} mins</strong></div></div></section><section className="card"><h2>Career history</h2><div className="timeline">{profile.experience.map((item) => <article key={item.role}><span className="timeline-dot" /><div><small>{item.period}</small><h3>{item.role}</h3><p>{item.employer}</p><p>{item.summary}</p></div></article>)}</div></section><section className="card"><h2>Target roles</h2><div className="skills">{profile.targetTitles.map((title) => <span className="pill target" key={title}>{title}</span>)}</div></section></div><aside className="jobs"><section className="card"><span className="eyebrow">Profile completeness</span><h2>Strong baseline</h2><div className="bar"><span style={{ width: "86%" }} /></div><p>Your experience, skills, salary and progression goals are ready for job matching.</p></section><section className="card"><h2>Verified skills</h2><div className="skills">{profile.skills.map((skill) => <span className="pill" key={skill}>{skill}</span>)}</div></section><section className="card"><h2>Career direction</h2><div className="skills">{profile.careerGoals.map((goal) => <span className="pill" key={goal}>{goal}</span>)}</div></section><section className="card"><span className="eyebrow">Agent guardrail</span><h2>Progression required</h2><p>Roles below your salary floor or without stronger cloud, infrastructure or leadership scope will be penalised.</p></section></aside></section></main>;
}
